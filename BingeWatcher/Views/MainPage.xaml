<Page
    x:Class="BingeWatcher.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">
    <Grid
        x:Name="ContentArea"
        Margin="{StaticResource MediumLeftRightMargin}" Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
        <StackPanel Grid.Column="6" Grid.Row="1"  Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Width="Auto" >
            <Button Content="Seriale" Margin="10,0,0,0" HorizontalAlignment="Center" Height="40"  Width="130" FontWeight="Bold" RenderTransformOrigin="1.069,-2" VerticalAlignment="Center">
                <Button.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="DarkOrange"/>
                        <GradientStop Color="White" Offset="4"/>
                    </LinearGradientBrush>
                </Button.Background>
            </Button>
            <Button Content="Filmy" Margin="10,0,0,0" Height="40"  Width="130" FontWeight="Bold" >
                <Button.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="DarkOrange"/>
                        <GradientStop Color="White" Offset="4"/>
                    </LinearGradientBrush>
                </Button.Background>
            </Button>

            <TextBox x:Name="wyszukaj" Text="wyszukaj"  Margin="10,0,0,0" Height="40"  Width="220"/>

            <Button Content="Szukaj" Margin="10,0,0,0" Height="40"  Width="100" FontWeight="Bold">
                <Button.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="DarkOrange"/>
                        <GradientStop Color="White" Offset="4"/>
                    </LinearGradientBrush>
                </Button.Background>
            </Button>

            <Button Content="Zarejestruj się" Margin="10,0,0,0" Height="40"  Width="130" FontWeight="Bold">
                <Button.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="DarkOrange"/>
                        <GradientStop Color="White" Offset="4"/>
                    </LinearGradientBrush>
                </Button.Background>
            </Button>
            <Button Content="Zaloguj" Margin="10,0,0,0" Height="40"  Width="130" FontWeight="Bold">
                <Button.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="DarkOrange"/>
                        <GradientStop Color="White" Offset="4"/>
                    </LinearGradientBrush>
                </Button.Background>
            </Button>
        </StackPanel>

        <!--    <Grid HorizontalAlignment="Center" Height="211" Width="42" Margin="0,0,0,0"/> -->

        <ListBox Margin="80,72,80,612" SelectionChanged="ListBox_SelectionChanged" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Path=TaskName}" />
                        <TextBlock Text="{Binding Path=Description}"/>
                        <TextBlock Text="{Binding Path=Priority}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <ListBox Margin="80,436,80,362" SelectionChanged="ListBox_SelectionChanged" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding TaskName}" />
                        <TextBlock Text="{Binding Description}"/>
                        <TextBlock Text="{Binding Priority}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ListBox Margin="80,696,80,102" SelectionChanged="ListBox_SelectionChanged" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding TaskName}" />
                        <TextBlock Text="{Binding Description}"/>
                        <TextBlock Text="{Binding Priority}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!--The SystemControlPageBackgroundChromeLowBrush background represents where you should place your content. 
                Place your content here.-->

    </Grid>
</Page>
